import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign-in - Academy Nexus | School Management Platform",
};

const LoginPage = () => {
  return <div>LoginPage</div>;
};

export default LoginPage;
