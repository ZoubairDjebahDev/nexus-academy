import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Parent - Academy Nexus | School Management Platform",
};

const ParentPage = () => {
  return <div>ParentPage</div>;
};

export default ParentPage;
